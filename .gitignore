# --- large / regenerable data (keep out of git) ---
data/maps/            # Vina precomputed affinity maps (~58 MB, regenerable)
data/raw/             # DUD-E download (~104 MB, regenerable)
data/_smoke_pose.sdf  # scratch
data/receptor.pdbqt   # regenerated by prep.py

# committed on purpose (do NOT ignore): data/docked_poses.sdf, data/manifest.csv,
# data/box.json, data/receptor_H.pdb  -> let run_analysis.py work on a fresh clone

# --- python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.ipynb_checkpoints/

# --- envs ---
.venv/
venv/
env/
.conda/

# --- logs / os ---
outputs/dock.log
*.log
.DS_Store
